# config.yaml - For TensorRT on Jetson
model_path: "models/best.engine"  # Changed from .pt or .onnx
source: 0                        # 0 for camera, or video file path
save_dir: "results"  
device: "cuda"                   # Will use CUDA
img_size: 640 
conf_threshold: 0.5 
iou_threshold: 0.5