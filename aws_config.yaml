# aws_config.yaml - THIS IS WHERE YOUR SECRETS GO
aws_region: "xx"  # Changed from s3_region
s3_bucket: "xx"

# AWS CREDENTIALS (FROM IAM USER)
aws_access_key_id: "xx"      # ← YOUR SECRET HERE
aws_secret_access_key: "xx"  # ← YOUR SECRET HERE

# RDS Database
rds_host: "xx"
rds_database: "xx"
rds_user: "xx"
rds_password: "xx"        # ← ANOTHER SECRET HERE
rds_port: xx